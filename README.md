# 分屏窗口切换器 · 使用手册

版本：2026-09-11 当前可用版  
原名称：Monitor Alt+Tab  
文件存放目录：`D:\autohotkey\script`  
适用环境：Windows 10 / Windows 11，AutoHotkey v2  
本机运行环境：AutoHotkey 2.0.27，双显示器

## 1. 这个工具能做什么

让 Alt+Tab 只在鼠标所在显示器的窗口之间切换。按住 Alt 时，屏幕中央出现带缩略图、图标和标题的选择器；选择目标后松开 Alt，或者直接点击卡片，即可激活该窗口。

这是免费的本地 AutoHotkey 脚本，不需要 DisplayFusion 或付费许可。脚本没有网络请求，不上传窗口标题和预览画面。

“分屏”指多个显示器之间分开切换窗口，不是把一个显示器划成若干区域。工具不会改变 Windows 的显示器布局。

## 2. 最常用的操作

1. 把鼠标移到需要切换窗口的显示器。
2. 按住 **Alt**，再按一下 **Tab**，显示窗口选择器。
3. 继续按 **Tab** 向后选择，或按 **Shift+Tab** 反向选择。
4. 查看亮蓝色边框、蓝色加粗标题和标题前的 **`>`**：它们标记松开 Alt 后将打开的窗口。
5. **松开 Alt**，确认并激活该窗口。

也可以保持 Alt 按住，直接用鼠标点击目标卡片的缩略图、标题或图标。点击后立即确认并关闭选择器，不需要再按 Tab。

| 操作 | 效果 |
| --- | --- |
| Alt+Tab | 打开选择器，向前选择 |
| 保持 Alt 按住，再按 Tab | 继续向前循环 |
| Alt+Shift+Tab | 反向循环 |
| 松开 Alt | 激活高亮条目 |
| 点击卡片 | 直接激活所点击的窗口 |
| Esc | 取消本次选择，不主动切换窗口 |
| 移动鼠标到另一块屏幕 | 下一轮 Alt+Tab 使用新屏幕；本轮不改变 |

鼠标仅悬停不会改变高亮。点击行为是“确认并打开”，不是仅移动选中框。松开 Alt 会立即结束选择器，因此需要鼠标选择时应继续按住 Alt。

## 3. 选择器界面

- **顶部 CURRENT DISPLAY**：表示当前锁定的显示器；后面的数字为“当前条目序号 / 总条目数”。
- **卡片**：应用图标、窗口标题、按比例缩放的窗口缩略图。
- **选中标记**：亮蓝色边框、蓝色加粗标题、标题前的 `>`。
- **底部页码**：当前页 / 总页数。
- **Preview unavailable**：无法获取该窗口的预览，仍可通过标题选择它。

选择器位于目标显示器可用区域中央，避开任务栏。每页最多 8 个条目，按屏幕空间减少行列；到达页尾继续按 Tab 会自动翻页。最后一页不足一页时可能保留空位，窗口尺寸保持稳定。

同页切换复用现有界面和缩略图，只更新高亮；翻页保留选择器窗口并更新内容。Windows 11 上会请求系统圆角效果。

## 4. 哪些窗口会出现

| 情况 | 当前规则 |
| --- | --- |
| 普通应用窗口 | 只列出属于目标显示器的候选窗口 |
| 跨越两块屏幕的窗口 | 归入与窗口相交面积最大的显示器 |
| 最小化窗口 | 按最小化前的位置归属；选中后恢复并激活 |
| 工具窗口、不可见窗口、不接受激活的窗口、子窗口 | 过滤掉 |
| 桌面、任务栏 | 过滤掉 |
| 被 Windows DWM 标记为隐藏的窗口 | 过滤掉，包含部分其他虚拟桌面窗口 |
| 带所有者的弹窗 | 尝试保留活动弹窗，减少重复条目 |
| 标题为空 | 尝试显示进程名称 |
| 取不到应用图标 | 使用通用图标 |
| 浏览器多个标签页 | 按浏览器窗口列出，不逐标签页列出 |

窗口顺序来自 Windows 的窗口堆叠顺序，近似最近使用顺序，不保证与系统原生 Alt+Tab 完全一致。

当前活动窗口属于目标屏幕时，首次 Alt+Tab 选择它之后的窗口；否则正向从第一个候选窗口开始，反向从最后一个开始。只有一个候选窗口时一直选中它，没有候选窗口时不显示选择器。

## 5. 一轮切换的处理流程

1. **确定显示器**：首次按 Alt+Tab 时读取鼠标位置，锁定目标显示器。
2. **建立候选列表**：枚举窗口，过滤不适合切换的窗口，筛选目标显示器的条目。
3. **确定初始选择**：结合当前活动窗口和正反向操作确定高亮条目。
4. **呈现界面**：创建卡片、加载图标、注册 DWM 缩略图，并显示高亮。
5. **继续选择**：每次 Tab 更新索引和高亮；跨页时更新卡片内容。
6. **确认**：松开 Alt 或点击卡片后，重新检查目标是否存在、可切换且仍在目标显示器。检查通过才恢复并激活。
7. **释放资源**：关闭选择器并释放缩略图资源；等待下一轮快捷键。

一轮中候选列表保持固定。中途新开的窗口下一轮才会加入；已关闭的窗口可能暂时保留条目，但不会被激活。目标已移到另一屏或变为不可切换状态时，本轮确认可能不产生切换，重新按 Alt+Tab 即可刷新。

## 6. 本机安装与自动启动

已配置为：**当前用户登录 Windows 后自动启动**。通常就是开机登录桌面后生效，无需每天双击脚本。不是登录前的系统服务，也不会给其他 Windows 用户自动启用。

| 项目 | 本机位置 |
| --- | --- |
| AutoHotkey 运行程序 | `D:\autohotkey\v2\AutoHotkey64.exe` |
| 当前生效脚本 | `D:\autohotkey\script\MonitorAltTab.ahk` |
| 自动启动快捷方式 | `C:\Users\EDY\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\分屏窗口切换器.lnk` |

自动启动快捷方式直接指定 AutoHotkey v2 和脚本路径。为了保持现有安装和更新路径稳定，脚本文件名仍保留 `MonitorAltTab.ahk`；手册和启动项使用中文名称“分屏窗口切换器”。托盘菜单中的英文名称 `Exit Monitor Alt+Tab` 也是这个工具。

**不要随意移动、删除或改名脚本文件，也不要移动 AutoHotkey 安装目录**，否则启动快捷方式需要同步修改。如果以后想把脚本整理到固定文件夹，应一起修改启动项中的路径。

脚本包含单实例设置：通过同一个路径重复启动会替换旧实例。不要运行其他目录里的旧版或备份脚本，以免多个不同路径的脚本争抢 Alt+Tab。

## 7. 手动启动、退出和取消自启

### 手动启动或重新加载

双击`D:\autohotkey\script\MonitorAltTab.ahk`。保存脚本修改后，再双击同一个文件即可重新加载。无需重新安装 AutoHotkey。

### 本次退出

在任务栏右下角的托盘区找到该脚本的 AutoHotkey 图标（可能在折叠图标中），右键，选择 **Exit Monitor Alt+Tab** 或 **Exit**。

退出后脚本不再拦截 Alt+Tab，系统恢复自行处理。单次退出不会取消自启，下次登录还会运行。

### 取消自动启动

1. 按 Win+R，输入 `shell:startup`，回车。
2. 删除其中的 **分屏窗口切换器** 快捷方式。
3. 如果还希望现在就停用，另外从托盘退出正在运行的脚本。

仅删除启动快捷方式不会删除脚本或卸载 AutoHotkey。要恢复自启，可以在这个启动文件夹中重新创建指向 AutoHotkey v2 和脚本的快捷方式。


### 自启的具体设置方式（当前已配置）

使用 Windows 当前用户的“启动”文件夹中的快捷方式实现，不使用计划任务或系统服务。

1. 按 **Win+R**，输入 `shell:startup`，回车，打开当前用户的启动文件夹。
2. 在文件夹空白处右键，选择 **新建 → 快捷方式**。
3. 在“对象的位置”中完整填写下面这一行，两个路径均保留双引号：

   ```text
   "D:\autohotkey\v2\AutoHotkey64.exe" "D:\autohotkey\script\MonitorAltTab.ahk"
   ```

4. 快捷方式名称填写 **分屏窗口切换器**，点击“完成”。已经有同名启动项时修改它即可，不要重复创建。
5. 右键快捷方式 → **属性**，确认“目标”为上述内容，“起始位置”为 `D:\autohotkey\script`。
6. 双击启动快捷方式可以立即验证能否运行；以后登录 Windows 会自动执行。同一路径脚本重复启动会替换原实例。

当前快捷方式已经按这些设置完成配置。脚本按当前用户普通权限启动；D 盘需要在登录时可用。若 Windows 的“启动应用”中禁用了该项，需要重新启用。退出当前脚本不影响下次登录自启；取消自启请删除此快捷方式。

脚本或 AutoHotkey 移动后，需要同步修改快捷方式的“目标”和“起始位置”。现在正式使用的三个文件为：

- `D:\autohotkey\script\MonitorAltTab.ahk`
- `D:\autohotkey\script\分屏窗口切换器-使用手册.md`
- `D:\autohotkey\script\分屏窗口切换器-使用手册.html`

桌面的手册快捷方式指向此目录中的 HTML 文件。

## 8. 常见问题

| 现象 | 处理方式 |
| --- | --- |
| 开机后没有生效 | 确认已登录桌面；检查启动快捷方式、脚本和运行程序仍存在；若 Windows 启动应用中被禁用，重新启用。先双击脚本文件检查能否手动运行 |
| 仍然显示系统 Alt+Tab | 检查脚本是否正在运行，以及有无其他键盘映射或窗口管理工具接管相同快捷键 |
| 高亮似乎没有变化 | 查看蓝色边框和标题前的 `>`；确认当前运行的是指定目录中的最新版；重新双击脚本文件加载 |
| 鼠标点击前界面就消失 | 点击期间保持 Alt 按住；松开 Alt 本身就会确认 |
| 出现另一块屏幕的跨屏窗口 | 按相交面积最大原则归属，将窗口完整移到所需屏幕后重新打开选择器 |
| 预览黑屏、冻结或不可用 | 通过图标和标题选择；特殊窗口的预览取决于 Windows 和应用 |
| 管理员程序无法切换 | 普通权限脚本可能受权限隔离限制。确有需要可手动以管理员身份运行脚本；当前自启不提升权限 |
| 脚本报错 | 保留弹窗的错误内容、行号和截图；先退出脚本以恢复系统处理，然后排查或恢复备份 |

## 9. 已知限制

- 选择器模仿 Windows 11 的卡片风格，但不是系统原生组件，没有完整复制动画、分组及内部筛选规则。
- 最小化窗口、受保护视频、部分 GPU 应用、远程桌面或独占全屏应用，可能出现黑屏或静止缩略图；接口成功不等于画面一定有效。
- 普通权限脚本对高权限应用的按键和激活可能受限，不能操作 UAC 安全桌面。
- 虚拟桌面、特殊弹窗、自定义外壳可能存在遗漏或重复，不能保证和系统原生列表完全一致。
- 混合 DPI、极小屏幕、显示器热插拔仍可能出现布局或归属问题；按 Esc 取消后重试。
- 不提供鼠标悬停选择、搜索、拖拽排序或设置界面；窗口较多或应用无响应时仍可能有延迟。
- Ctrl+Alt+Tab 等未定义的组合键不由本脚本接管。部分布局的右 Alt 是 AltGr，建议优先使用左 Alt。
- 不修改 Windows 多显示器设置；通常应使用“扩展这些显示器”。

## 10. 验证记录与版本整理

已完成基础运行测试：窗口过滤、双屏归属、正反向循环、分页、最小化恢复、激活和关闭清理共 24 项。后续增加了连续 40 次同页切换的界面和缩略图复用检查，以及预览、标题、图标三个区域的鼠标消息测试。高亮检查确认同一时间只有一个选中条目，且它与待激活目标一致。

上述自动化检查主要验证函数、资源和窗口消息，不覆盖所有真实键盘、鼠标和特殊应用场景。当前版本经用户实际体验反馈“目前看起来没有问题”。

本版已整合的修复包括：变量名冲突修复、单列提示优化、激活前校验、切换时界面复用、鼠标点击确认，以及高亮重绘与标题标记。自动启动项已创建并核对目标；未通过注销或重启电脑验证下一次登录，不会为验证而中断当前工作。

本手册为当前完整说明，替代早期简版《使用说明》。其他带 `before-` 的脚本是历史备份，不是日常启动入口。
